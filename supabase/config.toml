project_id = "eyliwidiljmzllsmytdh"

[functions]
  [functions.auth-email-hook]
    verify_jwt = false
  [functions.handle-email-suppression]
    verify_jwt = false
  [functions.handle-email-unsubscribe]
    verify_jwt = false
  [functions.preview-transactional-email]
    verify_jwt = false
  [functions.send-transactional-email]
    verify_jwt = true
  [functions.email-login]
    verify_jwt = false
  [functions.track-open]
    verify_jwt = false
  [functions.resume-site-pipeline]
    verify_jwt = true
  [functions.ingest-sourced-leads]
    # Authenticated with a timestamped HMAC from the private Lightsail worker.
    verify_jwt = false
  [functions.lead-scrape-status]
    # Authenticated with a timestamped HMAC from the private Lightsail worker.
    verify_jwt = false
  [functions.lead-sourcing]
    # Browser requests require a Supabase session; the function separately
    # verifies ownership before it can create or cancel a job.
    verify_jwt = true
